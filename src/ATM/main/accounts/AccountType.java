package main.accounts;

/**
 * Enum to replace the hard encoded strings for different account types
 */
public enum AccountType {
    CREDIT,
    LINE_CREDIT,
    CHEQUING,
    SAVINGS,
    OVERSEAS
}
